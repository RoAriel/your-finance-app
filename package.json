{
  "name": "your-finance-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:backend": "pnpm --filter backend dev",
    "dev:frontend": "pnpm --filter frontend dev",
    "dev:all": "pnpm -r dev",
    "dev": "concurrently --kill-others --names \"BACKEND,FRONTEND\" --prefix-colors \"blue,magenta\" \"cd apps/backend && pnpm run start:dev\" \"cd apps/frontend && pnpm run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
