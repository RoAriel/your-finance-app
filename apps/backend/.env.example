# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# Connection string para queries normales (Transaction mode con pooling)
# Formato: postgresql://postgres.PROJECT_ID:PASSWORD@REGION.pooler.supabase.com:6543/postgres?pgbouncer=true
DATABASE_URL="postgresql://postgres.xxxxx:your-password@aws-0-us-west-2.pooler.supabase.com:6543/postgres?pgbouncer=true"

# Connection string para migraciones (Session mode, conexión directa)
# Formato: postgresql://postgres.PROJECT_ID:PASSWORD@REGION.pooler.supabase.com:5432/postgres
DIRECT_URL="postgresql://postgres.xxxxx:your-password@aws-0-us-west-2.pooler.supabase.com:5432/postgres"

# ===========================================
# JWT CONFIGURATION
# ===========================================
# Secret key para firmar tokens JWT
# IMPORTANTE: Cambiar en producción por un string aleatorio de 32+ caracteres
# Generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="your-secret-key-change-in-production"

# Tiempo de expiración del token (ejemplos: 7d, 24h, 60m)
JWT_EXPIRES_IN="7d"

# ===========================================
# INSTRUCCIONES
# ===========================================
# 1. Copiar este archivo como .env:
#    cp .env.example .env
#
# 2. Reemplazar valores de Supabase:
#    - Ir a https://supabase.com/dashboard
#    - Seleccionar tu proyecto
#    - Settings > Database
#    - Copiar Connection String (URI mode)
#    - Para DATABASE_URL: usar puerto 6543 (Transaction mode)
#    - Para DIRECT_URL: usar puerto 5432 (Session mode)
#
# 3. Generar JWT_SECRET seguro:
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"